<Window x:Class="StudentRegistrationSystem.Views.StudentEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Student" Height="430" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Border Margin="18" Style="{StaticResource PanelBorderStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,2">
            <TextBlock Text="Last name" Width="140" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,2">
            <TextBlock Text="First name" Width="140" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,2">
            <TextBlock Text="Group" Width="140" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Group, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,2">
            <TextBlock Text="Study year (1..6)" Width="140" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox Text="{Binding StudyYear, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,2">
            <TextBlock Text="GPA (0..10)" Width="140" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox Text="{Binding GPA, Converter={StaticResource FlexibleDoubleConverter}, UpdateSourceTrigger=LostFocus}"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,2">
            <TextBlock Text="Scholarship" Width="140" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <CheckBox IsChecked="{Binding HasScholarship}"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,18,0,0">
            <Button Content="OK" IsDefault="True" Click="Ok_OnClick"/>
            <Button Content="Cancel" IsCancel="True" Style="{StaticResource SecondaryButtonStyle}"/>
        </StackPanel>
    </Grid>
    </Border>
</Window>
