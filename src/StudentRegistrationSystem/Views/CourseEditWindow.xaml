<Window x:Class="StudentRegistrationSystem.Views.CourseEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Course" Height="360" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Border Margin="18" Style="{StaticResource PanelBorderStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,2">
            <TextBlock Text="Title" Width="140" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,2">
            <TextBlock Text="Department" Width="140" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Department, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,2">
            <TextBlock Text="Credits" Width="140" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Credits, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,2">
            <TextBlock Text="Max students" Width="140" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <TextBox Text="{Binding MaxStudents, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,18,0,0">
            <Button Content="OK" IsDefault="True" Click="Ok_OnClick"/>
            <Button Content="Cancel" IsCancel="True" Style="{StaticResource SecondaryButtonStyle}"/>
        </StackPanel>
    </Grid>
    </Border>
</Window>
