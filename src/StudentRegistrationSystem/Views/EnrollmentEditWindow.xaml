<Window x:Class="StudentRegistrationSystem.Views.EnrollmentEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:StudentRegistrationSystem.Models"
        Title="Enrollment" Height="360" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="Student:" Width="140" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Students}"
                      SelectedItem="{Binding SelectedStudent}"
                      DisplayMemberPath="FullName"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Course:" Width="140" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding Courses}"
                      SelectedItem="{Binding SelectedCourse}"
                      DisplayMemberPath="Title"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBlock Text="Registration date:" Width="140" VerticalAlignment="Center"/>
            <DatePicker SelectedDate="{Binding Enrollment.RegisteredAt}"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <TextBlock Text="Status:" Width="140" VerticalAlignment="Center"/>
            <ComboBox ItemsSource="{Binding StatusValues}"
                      SelectedItem="{Binding Enrollment.Status}"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal">
            <TextBlock Text="Grade (0..10):" Width="140" VerticalAlignment="Center"/>
            <TextBox Text="{Binding Enrollment.Grade, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" IsDefault="True" Click="Ok_OnClick"/>
            <Button Content="Cancel" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
