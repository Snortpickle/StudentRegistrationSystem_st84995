<Window x:Class="StudentRegistrationSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:StudentRegistrationSystem.ViewModels"
        Title="Login" Height="220" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Sign in (demo: admin / admin)" FontWeight="SemiBold" FontSize="14"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="Login:" VerticalAlignment="Center" Width="100"/>
            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="Password:" VerticalAlignment="Center" Width="100"/>
            <PasswordBox x:Name="Pwd" PasswordChanged="PasswordBox_OnPasswordChanged"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Sign in" Command="{Binding TryLoginCommand}"/>
        </StackPanel>
    </Grid>
</Window>
